# AATM Configuration
# Copy this file to .env and adjust values as needed

# Path to your media files on the host machine (with write access for creating torrents/nfo)
# This will be mounted as /media inside the container
# The full filesystem is also mounted read-only at /host for navigation
MEDIA_PATH=/

# API port (default: 8085)
AATM_API_PORT=8085

# qBittorrent WebUI port (default: 8086)
AATM_QBIT_PORT=8086

# Timezone (default: Europe/Paris)
TZ=Europe/Paris
